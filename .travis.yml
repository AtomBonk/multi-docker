sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t atombonk/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true atombonk/react-test npm test

after_success:
  - docker build -t atombonk/multi-client ./client
  - docker build -t atombonk/multi-nginx ./nginx
  - docker build -t atombonk/multi-server ./server
  - docker build -t atombonk/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push atombonk/multi-client
  - docker push atombonk/multi-nginx
  - docker push atombonk/multi-server
  - docker push atombonk/multi-worker
